Config = {
    -- General settings
    isUnitMetric = true, -- false if you want to use imperial units
    
    -- Update intervals (milliseconds)
    updateInterval = 500, -- Default update interval
    vehicleUpdateInterval = 100, -- Update interval when in vehicle
    
    -- Display settings
    hideInPauseMenu = true, -- Hide HUD when pause menu is active
    
    -- Custom colors (can be used in UI)
    colors = {
        health = "#f01647",
        armour = "#4589ef",
        hunger = "#ffb54b",
        thirst = "#60e3ec",
        stress = "#5866de",
        energy = "#5866de"
    },
} 